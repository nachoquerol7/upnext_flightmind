[pytest]
timeout = 30
markers =
    slow: tests que arrancan nodos ROS2 reales
    tight_dwell: FSM conserva max_duration_sec del YAML (solo M2 morada)
    demo: tests incluidos en la suite de demo para el 8 de abril
